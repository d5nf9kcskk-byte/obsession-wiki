# Copy to `.env` for local development.
#
# URL of the deployed Cloudflare Worker proxy (see worker/README.md).
# The "Ask the Rulebook" chat sends requests here instead of calling Anthropic
# directly, so the API key stays server-side.
VITE_AI_PROXY_URL=https://obsession-ai-proxy.your-subdomain.workers.dev
